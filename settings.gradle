pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id 'io.micronaut.application' version '4.5.3' // Use the latest version
        id 'io.micronaut.application' version '4.6.0' // Use the latest version
    }
}
